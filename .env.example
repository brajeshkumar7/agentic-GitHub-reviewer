# Leave blank for offline tests. Only Groq needs credentials; DDGS search does not.
GROQ_API_KEY=
GROQ_MODEL=
AGENT_INJECT_FAILURE=false
# Valid modes: tool_timeout, malformed_tool_response
AGENT_FAILURE_MODE=
# Optional; valid tools: web_search, url_fetch, calculator
AGENT_FAILURE_TOOL=
