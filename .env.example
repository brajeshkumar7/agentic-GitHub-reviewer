# Leave blank for offline tests. Set credentials only when enabling live providers.
GROQ_API_KEY=
GROQ_MODEL=
BRAVE_SEARCH_API_KEY=
AGENT_INJECT_FAILURE=false
# Valid modes: tool_timeout, malformed_tool_response
AGENT_FAILURE_MODE=
# Optional; valid tools: web_search, url_fetch, calculator
AGENT_FAILURE_TOOL=
